<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Comprobante</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: Arial, sans-serif;
      font-size: 12px;
      padding: 20px;
      line-height: 1.4;
    }
    .header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 20px;
    }
    .logo-empresa {
      display: flex;
      align-items: flex-start;
      flex: 1;
    }
    .logo-empresa img {
      width: 120px;
      height: 120px;
      margin-right: 15px;
      object-fit: contain;
    }
    .empresa-info h1 {
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 5px;
    }
    .empresa-info p {
      font-size: 11px;
      line-height: 1.6;
    }
    .documento {
      border: 1px solid #000;
      padding: 15px 20px;
      text-align: center;
      min-width: 200px;
    }
    .documento .ruc {
      font-size: 11px;
      margin-bottom: 5px;
    }
    .documento .tipo {
      font-size: 18px;
      font-weight: bold;
      margin: 5px 0;
    }
    .documento .electronica {
      font-size: 18px;
      font-weight: bold;
      margin: 5px 0;
    }
    .documento .numero {
      font-size: 14px;
      margin-top: 5px;
    }
    .cliente-section {
      display: flex;
      justify-content: space-between;
      margin: 20px 0;
    }
    .cliente-left, .cliente-right {
      flex: 1;
    }
    .cliente-left label, .cliente-right label {
      display: flex;
      font-size: 11px;
      margin-top: 5px;
    }
    .cliente-left label div:first-child,
    .cliente-right label div:first-child {
      font-weight: bold;
      min-width: 100px;
    }
    .cliente-right label div:first-child {
      min-width: 110px;
    }
    .productos-table {
      width: 100%;
      border: 1px solid #222;
      border-collapse: collapse;
      margin-top: 10px;
    }
    .productos-table thead {
      border-bottom: 1px solid #222;
    }
    .productos-table th {
      padding: 5px;
      font-size: 11px;
      font-weight: bold;
      text-align: center;
    }
    .productos-table td {
      padding: 5px;
      font-size: 11px;
    }
    .productos-table .cant {
      width: 16.66%;
      text-align: center;
    }
    .productos-table .um {
      width: 16.66%;
      text-align: center;
    }
    .productos-table .desc {
      width: 50%;
      text-align: left;
    }
    .productos-table .pu {
      width: 16.66%;
      text-align: left;
    }
    .productos-table .total {
      width: 16.66%;
      text-align: right;
    }
    .totales-section {
      display: flex;
      justify-content: space-between;
      margin-top: 15px;
    }
    .son {
      flex: 1;
      font-size: 11px;
      font-weight: bold;
    }
    .total-importe {
      text-align: right;
    }
    .total-importe p {
      font-size: 11px;
      margin: 3px 0;
    }
    .total-importe .total-final {
      font-weight: bold;
    }
    .observaciones {
      border: 1px solid #000;
      padding: 10px;
      margin-top: 15px;
      position: relative;
      min-height: 100px;
    }
    .observaciones p {
      font-size: 11px;
      line-height: 1.5;
    }
    .observaciones .qr {
      position: absolute;
      bottom: 10px;
      right: 10px;
      width: 60px;
      height: 60px;
    }
  </style>
</head>
<body>
  <!-- Header con logo, empresa y documento -->
  <div class="header">
    <div class="logo-empresa">
      {{#if logo}}
      <img src="{{logo}}" alt="Logo">
      {{/if}}
      <div class="empresa-info">
        <h1>{{nombreComercial}}</h1>
        <p>
          {{direccion}}<br>
          {{rubro}}<br>
          RAZON SOCIAL: {{razonSocial}}<br>
          CELULAR: {{celular}}<br>
          EMAIL: {{email}}
        </p>
      </div>
    </div>
    <div class="documento">
      <div class="ruc">RUC: {{ruc}}</div>
      <div class="tipo">{{tipoDocumento}}</div>
      <div class="electronica">ELECTRONICA</div>
      <div class="numero">{{serie}}-{{correlativo}}</div>
    </div>
  </div>

  <!-- Información del cliente -->
  <div class="cliente-section">
    <div class="cliente-left">
      <label><div>CLIENTE:</div><div>{{clienteNombre}}</div></label>
      <label><div>DIRECCION:</div><div>{{clienteDireccion}}</div></label>
      <label><div>FORMA PAGO:</div><div>{{formaPago}}</div></label>
      <label><div>MEDIO PAGO:</div><div>{{medioPago}} S/ {{mtoImpVenta}}</div></label>
    </div>
    <div class="cliente-right">
      <label><div>RUC:</div><div>{{clienteNumDoc}}</div></label>
      <label><div>FECHA:</div><div>{{fecha}}</div></label>
      <label><div>HORA:</div><div>{{hora}}</div></label>
      <label><div>MONEDA:</div><div>SOLES</div></label>
    </div>
  </div>

  <!-- Tabla de productos -->
  <table class="productos-table">
    <thead>
      <tr>
        <th class="cant">CANT.</th>
        <th class="um">U.M.</th>
        <th class="desc">DESCRIPCION</th>
        <th class="pu">P.U.</th>
        <th class="total">IMPORTE</th>
      </tr>
    </thead>
    <tbody>
      {{#each productos}}
      <tr>
        <td class="cant">{{cantidad}}</td>
        <td class="um">{{unidadMedida}}</td>
        <td class="desc">{{descripcion}}</td>
        <td class="pu">{{precioUnitario}}</td>
        <td class="total">{{total}}</td>
      </tr>
      {{/each}}
    </tbody>
  </table>

  <!-- Totales -->
  <div class="totales-section">
    <div class="son">SON: {{totalEnLetras}}</div>
    <div class="total-importe">
      <p class="total-final"><span style="font-weight: bold;">IMPORTE TOTAL:</span> S/ {{mtoImpVenta}}</p>
    </div>
  </div>

  <!-- Observaciones y QR -->
  <div class="observaciones">
    <p>
      <span style="font-weight: bold;">OBSERVACIONES:</span><br>
      {{#if observaciones}}{{observaciones}}<br>{{/if}}
      Representación impresa del Comprobante de Pago Electrónico.<br>
      Autorizado mediante Resolución de Intendencia N° 080-005-000153/SUNAT.<br>
      Emite a través de APISPERU - Proveedor Autorizado por SUNAT.
    </p>
    {{#if qrCode}}
    <img src="{{qrCode}}" alt="QR" class="qr">
    {{/if}}
  </div>
</body>
</html>
